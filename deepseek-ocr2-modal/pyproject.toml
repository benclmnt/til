[project]
name = "deepseek-ocr2-modal"
version = "0.1.0"
description = "Deploy DeepSeek-OCR-2 (vLLM) on Modal."
requires-python = ">=3.11"
dependencies = [
    "modal>=1.3",
    "httpx>=0.27",
]

[tool.uv]
package = false

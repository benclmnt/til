[project]
name = "paddleocr-modal"
version = "0.1.0"
description = "Local tooling to deploy PaddleOCR on Modal (modal CLI)."
requires-python = ">=3.11"
dependencies = [
    "modal>=1.3",
    "httpx>=0.27",
]

[tool.uv]
package = false
